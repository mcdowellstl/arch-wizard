{
  "name": "arch-wizard",
  "version": "1.0.0",
  "description": "Restaurant hardware support diagnostic tool",
  "main": "server.js",
  "scripts": {
    "api": "node server.js",
    "client": "npm run dev --prefix client",
    "dev": "concurrently -n API,UI -c cyan,magenta \"npm run api\" \"npm run client\"",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": [
    "restaurant",
    "support",
    "diagnostics"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
